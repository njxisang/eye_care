name: eye_care
description: "护眼宝 - 色温调节、亮度控制、护眼提醒、使用时长统计"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.5
  sqflite: ^2.4.1
  path_provider: ^2.1.5
  fl_chart: ^0.70.2
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
